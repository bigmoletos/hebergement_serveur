tcp:
  routers:
    ssh:
      entryPoints:
        - "ssh"
      rule: "HostSNI(`*`)"
      service: ssh
      tls: {}

  services:
    ssh:
      loadBalancer:
        servers:
          - address: "ubuntu:22"